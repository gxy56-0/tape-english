================================================================================
  HK English Typing Practice — 更新日志 (CHANGELOG)
  最后更新: 2026-05-17
================================================================================

【版本概述】
  香港英语打字练习网站，单文件 HTML 应用。支持粤语发音、粤拼显示、
  716 词库、大小写不敏感、6 套背景主题 + 取色器自动配色。

================================================================================
  v2.5 — 主题系统重写 + 取色器自动配色
================================================================================

1. 修复主题切换不生效的 Bug
   - 原因: document.documentElement.style.setProperty() 在部分浏览器中优先级
          低于样式表中的 :root 声明
   - 解决: 改为在 <head> 中动态注入 <style id="theme-style"> 标签，
          用 textContent 覆写 :root 变量，利用 CSS 层叠顺序确保生效

2. 保留 6 套预设主题 (presetThemes)
   - Cyberpunk (青蓝霓虹)
   - Matrix    (荧光绿)
   - Sunset    (暖橙暗金)
   - Ocean     (深海蓝)
   - Sakura    (樱花粉)
   - Minimal   (极简黑白)

3. 新增取色器 + 自动配色系统
   - 添加七彩渐变圆点 (theme-dot-picker)，内嵌 <input type="color">
   - 用户选择任意颜色后，HSL 算法自动生成全套 12 色配色方案:
      背景色 (bg):          同色相, 18%饱和度, 5%亮度
      表面色 (surface):     同色相, 25%饱和度, 9%亮度
      替代表面 (surfaceAlt):同色相, 30%饱和度, 15%亮度
      主强调 (cyan):        用户选择的颜色
      品红 (magenta):       色相 +30°
      紫色 (purple):        色相 +80°
      绿色 (green):         色相 +140°
      红色 (red):           色相 +190°
      琥珀 (amber):         色相 +45°
      文字 (text):          同色相, 8%饱和度, 90%亮度
      弱文字 (muted):       同色相, 12%饱和度, 50%亮度
      边框 (border):        同色相, 22%饱和度, 16%亮度
   - 新增函数: hexToHsl(), hslToHex(), generateThemeFromColor(), applyCustomColor()
   - 自定义主题保存到 localStorage (typing_custom_theme / typing_custom_hex)

4. 顶栏新增 T 按钮
   - 点击在 6 套预设主题间循环切换 (cycleTheme)

5. 更新 init() 逻辑
   - 启动时从 localStorage 恢复自定义主题和取色器值
   - 排除 .theme-dot-picker 避免重复绑定点击事件

================================================================================
  v2.4 — 数量选择面板调整
================================================================================

1. 数量选项改为: 10 / 30 / 50 / Custom (默认 30)
2. 新增自定义数量输入框 (.count-custom-input)
   - 点击 Custom 显示 <input type="number">
   - 自动聚焦，用户可输入任意数字
3. 新增 .count-custom-wrap 和 .count-custom-input 的 CSS 样式
4. 更新 count selector JS 逻辑
   - isCustom 标记
   - 自定义模式下从输入框取值
   - 非自定义模式下选择按钮值

================================================================================
  v2.3 — 词汇库扩充至 716 词 + 粤拼
================================================================================

1. 词汇库从 ~60 词扩充至 716 词
   - workplace: ~250 词 (办公、金融、法律、科技、管理)
   - daily:     ~466 词 (饮食、地点、交通、天气、节庆、住房、社媒、日常用语)

2. 每个词汇条目新增 jyut 字段 (粤拼 / Jyutping)
   - 格式: { en: "cha chaan teng", zh: "茶餐厅", jyut: "caa4 caan1 teng1" }
   - 使用香港语言学学会标准粤拼方案
   - zh 字段中 "/" 全部替换为空格 (防止 TTS 读出"斜线")

3. 新增粤拼显示元素
   - HTML: <div class="word-jyutping" id="wordJyutping">
   - CSS: 14px 紫色等宽字体, 不透明度 0.7
   - JS: renderWord() 中设置 wordJyutping.textContent
   - resetSession() 中清空 wordJyutping

4. 中文字体放大
   - .word-translation: 15px → 26px, 加粗, 不透明度 0.9
   - 新增 .word-jyutping 样式

================================================================================
  v2.2 — 每日学习数量选择
================================================================================

1. 新增数量选择面板 (.count-overlay)
   - 在 Start Overlay 之前显示
   - 选项: 10 / 30 / 50 / Custom (带输入框)
   - 显示总词库数量
   - START 按钮确认

2. CSS: .count-overlay, .count-card, .count-options, .count-opt, .count-confirm

3. JS 逻辑
   - state.dailyCount: 0 = 全部, >0 = 限制数量
   - setupCountSelector() 在 init() 中调用
   - 确认后随机抽取对应数量词汇
   - resetSession() 尊重 dailyCount 限制

================================================================================
  v2.1 — 多词条目空格 + 粤语发音 + 手动播放
================================================================================

1. 多词条目空格跳过逻辑
   - 问题: "cha chaan teng" 需要按空格才能匹配
   - 新增辅助函数:
       typableLength(word)      — 去掉空格后的可打字字符数
       nthNonSpace(word, n)    — 第 n 个非空格字符在原词中的索引
       nonSpaceCountBefore(w,i) — i 位置之前的非空格字符数
   - renderWord(): 空格字符渲染为 .letter.space 类 (视觉保持间距)
   - handleKeydown(): 用 nthNonSpace 映射用户输入到单词位置
   - handleCorrectLetter(): 用 typableLength 判断单词完成
   - updateLetterStates(): 空格跳过颜色判断
   - 启动按键处理同样适配

2. 粤语发音
   - speakCantonese() 函数
   - 优先 Web Speech API zh-HK (Windows: Microsoft Tracy)
   - 兜底 Google Translate TTS (tl=zh-HK)
   - speakWord() 重构: 英语朗读结束后 onend 自动链式调用 speakCantonese
   - zh 字段中 "/" 替换为空格后再朗读

3. 手动发音按钮
   - 位置: 单词显示和进度条之间 (.pronounce-row)
   - EN 按钮: speakEnglish() — 播放英语发音
   - 粤 按钮: speakCantonese() — 播放粤语发音
   - 不受静音开关影响
   - speakEnglish/speakCantonese 均加 speechSynthesis.cancel()

================================================================================
  v2.0 — 大小写不敏感 + 自动发音 + 单词列表
================================================================================

1. 大小写不敏感匹配 (3 处)
   - updateLetterStates: userInput[idx].toLowerCase() === word.en[idx].toLowerCase()
   - handleKeydown: e.key.toLowerCase() === word.en[idx].toLowerCase()
   - 启动首字母: e.key.toLowerCase() === word.en[0].toLowerCase()

2. 自动发音 (Web Speech API)
   - speakWord(): SpeechSynthesisUtterance, lang="en-US", rate=0.85
   - 每次 renderWord 时自动朗读
   - 顶栏 S 按钮切换静音 (toggleSound)
   - 兼容性: window.speechSynthesis 不可用时静默跳过

3. 单词列表 + 学习统计
   - localStorage 持久化: typing_word_stats
   - 每个词记录 attempts (尝试次数) 和 completed (完成次数)
   - renderWord() 中 recordAttempt(), advanceWord() 中 recordCompletion()
   - 顶栏 W 按钮打开单词列表面板 (.wordlist-overlay)
   - 表格: Word / Translation / Category / Attempts / Completed
   - 点击表头排序 (wordListSort)
   - 关闭方式: X / 背景点击 / Escape / 按字母键

================================================================================
  v1.0 — 初始版本
================================================================================

1. 基础架构
   - 单文件 HTML (index.html)
   - 赛博朋克暗色主题 + 星场背景 + 扫描线效果
   - 自定义 CSS 变量 (:root)
   - 响应式设计 (@media max-width: 640px)

2. 核心功能
   - 词库: workplace (30词) + daily (30词)
   - 分类切换: All / Workplace / Daily
   - 实时统计: Time / Inputs / WPM / Correct / Accuracy
   - 进度条
   - 结果面板 (WPM / Accuracy / Time / Words / Errors)
   - Retry / New Words 按钮
   - Start Overlay (按任意键开始)

3. 交互
   - 隐藏输入框 (hidden input) 捕获键盘
   - 逐字母高亮 (correct / wrong / normal)
   - 错误时抖动动画 + 350ms 后重置
   - 正确完成单词后 150ms 延迟推进

================================================================================
【技术栈】
  - 纯 HTML + CSS + JavaScript (无框架、无依赖)
  - Web Speech API (发音)
  - localStorage (主题偏好、单词统计、自定义主题)
  - HSL 色彩算法 (自动配色)

【文件】
  index.html  — 主应用文件 (~2623 行，单文件)

【待办 / 已知限制】
  - 无测试覆盖
  - 取色器在某些移动浏览器可能不支持
  - Google TTS 兜底方案可能有 CORS 限制
